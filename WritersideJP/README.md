# WritersideJP

Japanese documentation module. Built as instance `jp` and published under
`/jp` on GitHub Pages.

Topic file names mirror `Writerside/topics` so the language switcher can map
any English page to its Japanese counterpart by inserting `/jp/` in the path.

Until pages are translated, topics may still contain English text. Replace
files in place; do not rename them.
